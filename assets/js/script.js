/* -------------------------- SELECT ITEM ---------------------------*/

/* calling all functions */

selectItem(foodElements);
selectItem(drinkElements);
selectItem(dessertElements);

setInterval (isOrderCompleted, 500); // call isOrderCompleted on a set interval (500ms)

modalWindow();